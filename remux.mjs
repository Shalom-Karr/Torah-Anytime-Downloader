// Generated from scripts/remux-taRx.js — the fMP4/TS -> progressive-MP4 transmuxer,
// served to the page at /__ta__remux.js and loaded on demand by the downloader.
export const REMUX_JS = "// ===================================================================\n// taRx: HLS (fMP4/CMAF + MPEG-TS) -> flat progressive MP4 remuxer.\n// Self-contained, no imports/exports. Entry: taRxRemux(initSeg, segments)\n// ===================================================================\n\n// ---- exp-golomb: bit reader + H.264 SPS parser ---------------------\n\nclass taRxBitReader {\n  constructor(bytes) {\n    this.data = bytes;\n    this.bytePos = 0;\n    this.bitPos = 0;\n  }\n  bit() {\n    const b = this.data[this.bytePos];\n    const val = (b >> (7 - this.bitPos)) & 1;\n    if (++this.bitPos === 8) { this.bitPos = 0; this.bytePos++; }\n    return val;\n  }\n  bits(n) {\n    let v = 0;\n    for (let i = 0; i < n; i++) v = (v << 1) | this.bit();\n    return v >>> 0;\n  }\n  ue() {\n    let zeros = 0;\n    while (this.bytePos < this.data.length && this.bit() === 0) zeros++;\n    if (zeros === 0) return 0;\n    return ((1 << zeros) >>> 0) - 1 + this.bits(zeros);\n  }\n  se() {\n    const v = this.ue();\n    return (v & 1) ? (v + 1) >>> 1 : -(v >>> 1);\n  }\n}\n\nfunction taRxUnescapeRbsp(nal) {\n  const out = [];\n  for (let i = 0; i < nal.length; i++) {\n    if (i >= 2 && nal[i] === 0x03 && nal[i - 1] === 0x00 && nal[i - 2] === 0x00 &&\n        i + 1 < nal.length && nal[i + 1] <= 0x03) {\n      continue;\n    }\n    out.push(nal[i]);\n  }\n  return new Uint8Array(out);\n}\n\nfunction taRxParseSps(nalWithHeader) {\n  const rbsp = taRxUnescapeRbsp(nalWithHeader.subarray(1));\n  const r = new taRxBitReader(rbsp);\n\n  const profileIdc = r.bits(8);\n  r.bits(8);\n  const levelIdc = r.bits(8);\n  r.ue();\n\n  let chromaFormatIdc = 1;\n  if ([100, 110, 122, 244, 44, 83, 86, 118, 128, 138, 139, 134, 135].includes(profileIdc)) {\n    chromaFormatIdc = r.ue();\n    if (chromaFormatIdc === 3) r.bit();\n    r.ue();\n    r.ue();\n    r.bit();\n    if (r.bit()) {\n      const n = chromaFormatIdc !== 3 ? 8 : 12;\n      for (let i = 0; i < n; i++) {\n        if (r.bit()) {\n          const size = i < 6 ? 16 : 64;\n          let lastScale = 8, nextScale = 8;\n          for (let j = 0; j < size; j++) {\n            if (nextScale !== 0) nextScale = (lastScale + r.se() + 256) % 256;\n            lastScale = nextScale === 0 ? lastScale : nextScale;\n          }\n        }\n      }\n    }\n  }\n\n  r.ue();\n  const picOrderCntType = r.ue();\n  if (picOrderCntType === 0) {\n    r.ue();\n  } else if (picOrderCntType === 1) {\n    r.bit();\n    r.se();\n    r.se();\n    const n = r.ue();\n    for (let i = 0; i < n; i++) r.se();\n  }\n\n  r.ue();\n  r.bit();\n  const picWidthInMbsMinus1 = r.ue();\n  const picHeightInMapUnitsMinus1 = r.ue();\n  const frameMbsOnlyFlag = r.bit();\n  if (!frameMbsOnlyFlag) r.bit();\n  r.bit();\n\n  let cropLeft = 0, cropRight = 0, cropTop = 0, cropBottom = 0;\n  if (r.bit()) {\n    cropLeft = r.ue();\n    cropRight = r.ue();\n    cropTop = r.ue();\n    cropBottom = r.ue();\n  }\n\n  const subWidthC = chromaFormatIdc === 1 || chromaFormatIdc === 2 ? 2 : 1;\n  const subHeightC = chromaFormatIdc === 1 ? 2 : 1;\n  const cropUnitX = chromaFormatIdc === 0 ? 1 : subWidthC;\n  const cropUnitY = (chromaFormatIdc === 0 ? 1 : subHeightC) * (2 - frameMbsOnlyFlag);\n\n  const width = (picWidthInMbsMinus1 + 1) * 16 - (cropLeft + cropRight) * cropUnitX;\n  const height = (2 - frameMbsOnlyFlag) * (picHeightInMapUnitsMinus1 + 1) * 16\n               - (cropTop + cropBottom) * cropUnitY;\n\n  return { width, height, profileIdc, levelIdc };\n}\n\n// ---- fragmented-MP4 (CMAF) demuxer ---------------------------------\n\nconst taRxSAMPLE_RATES = [96000, 88200, 64000, 48000, 44100, 32000, 24000, 22050,\n  16000, 12000, 11025, 8000, 7350, 0, 0, 0];\n\nfunction taRxBoxes(bytes, start, end) {\n  const dv = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);\n  const out = [];\n  let o = start;\n  while (o + 8 <= end) {\n    let size = dv.getUint32(o);\n    const type = String.fromCharCode(bytes[o + 4], bytes[o + 5], bytes[o + 6], bytes[o + 7]);\n    let header = 8;\n    if (size === 1) { size = dv.getUint32(o + 8) * 4294967296 + dv.getUint32(o + 12); header = 16; }\n    else if (size === 0) size = end - o;\n    if (size < header || o + size > end) break;\n    out.push({ type, start: o, body: o + header, end: o + size });\n    o += size;\n  }\n  return out;\n}\n\nfunction taRxFindBox(bytes, start, end, type) {\n  return taRxBoxes(bytes, start, end).find((b) => b.type === type);\n}\n\nfunction taRxParseAvcC(bytes, b) {\n  let o = b.body;\n  o += 5;\n  const numSps = bytes[o++] & 0x1f;\n  let sps = null, pps = null;\n  const dv = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);\n  for (let i = 0; i < numSps; i++) { const len = dv.getUint16(o); o += 2; if (!sps) sps = bytes.slice(o, o + len); o += len; }\n  const numPps = bytes[o++];\n  for (let i = 0; i < numPps; i++) { const len = dv.getUint16(o); o += 2; if (!pps) pps = bytes.slice(o, o + len); o += len; }\n  return { sps, pps };\n}\n\nfunction taRxParseEsdsConfig(bytes, b) {\n  let o = b.body + 4;\n  const readLen = () => { let n = 0, x; do { x = bytes[o++]; n = (n << 7) | (x & 0x7f); } while (x & 0x80); return n; };\n  if (bytes[o++] !== 0x03) return null; readLen();\n  o += 2;\n  const flags = bytes[o++];\n  if (flags & 0x80) o += 2;\n  if (flags & 0x40) o += 1 + bytes[o];\n  if (flags & 0x20) o += 2;\n  if (bytes[o++] !== 0x04) return null; readLen();\n  o += 13;\n  if (bytes[o++] !== 0x05) return null;\n  const len = readLen();\n  return bytes.slice(o, o + len);\n}\n\nfunction taRxParseTrak(bytes, trak) {\n  const dv = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);\n  const tkhd = taRxFindBox(bytes, trak.body, trak.end, \"tkhd\");\n  const mdia = taRxFindBox(bytes, trak.body, trak.end, \"mdia\");\n  if (!mdia) return null;\n  const mdhd = taRxFindBox(bytes, mdia.body, mdia.end, \"mdhd\");\n  const hdlr = taRxFindBox(bytes, mdia.body, mdia.end, \"hdlr\");\n  const minf = taRxFindBox(bytes, mdia.body, mdia.end, \"minf\");\n  if (!mdhd || !hdlr || !minf) return null;\n\n  const mdhdV = bytes[mdhd.body];\n  const timescale = mdhdV === 1 ? dv.getUint32(mdhd.body + 20) : dv.getUint32(mdhd.body + 12);\n  const handler = String.fromCharCode(bytes[hdlr.body + 8], bytes[hdlr.body + 9], bytes[hdlr.body + 10], bytes[hdlr.body + 11]);\n\n  const tkhdV = tkhd ? bytes[tkhd.body] : 0;\n  const trackId = tkhd ? (tkhdV === 1 ? dv.getUint32(tkhd.body + 20) : dv.getUint32(tkhd.body + 12)) : 1;\n\n  const stbl = taRxFindBox(bytes, minf.body, minf.end, \"stbl\");\n  const stsd = stbl && taRxFindBox(bytes, stbl.body, stbl.end, \"stsd\");\n  if (!stsd) return null;\n  const entry = taRxBoxes(bytes, stsd.body + 8, stsd.end)[0];\n  if (!entry) return null;\n\n  if (handler === \"vide\") {\n    let width = dv.getUint16(entry.body + 24);\n    let height = dv.getUint16(entry.body + 26);\n    const avcC = taRxFindBox(bytes, entry.body + 78, entry.end, \"avcC\");\n    if (!avcC) return null;\n    const { sps, pps } = taRxParseAvcC(bytes, avcC);\n    if (!sps || !pps) return null;\n    const nalLen = (bytes[avcC.body + 4] & 0x03) + 1;\n    if (!width || !height) { try { const d = taRxParseSps(sps); width = d.width; height = d.height; } catch (e) {} }\n    return { trackId, kind: \"video\", timescale, width, height, sps, pps, nalLen, samples: [] };\n  }\n  if (handler === \"soun\") {\n    const channels = dv.getUint16(entry.body + 16);\n    const esds = taRxFindBox(bytes, entry.body + 28, entry.end, \"esds\");\n    const config = esds ? taRxParseEsdsConfig(bytes, esds) : null;\n    if (!config) return null;\n    const srIndex = ((config[0] & 0x07) << 1) | ((config[1] >> 7) & 0x01);\n    const sampleRate = taRxSAMPLE_RATES[srIndex] || dv.getUint16(entry.body + 24) || 44100;\n    return { trackId, kind: \"audio\", timescale, channels: channels || (((config[1] >> 3) & 0x0f)), sampleRate, config, samples: [] };\n  }\n  return null;\n}\n\nfunction taRxNormalizeNals(data, nalLen) {\n  if (nalLen === 4) return data;\n  const parts = [];\n  let o = 0, total = 0;\n  while (o + nalLen <= data.length) {\n    let len = 0;\n    for (let k = 0; k < nalLen; k++) len = (len << 8) | data[o + k];\n    o += nalLen;\n    const nal = data.subarray(o, o + len); o += len;\n    parts.push(nal); total += 4 + nal.length;\n  }\n  const out = new Uint8Array(total);\n  let p = 0;\n  for (const nal of parts) {\n    out[p] = (nal.length >>> 24) & 0xff; out[p + 1] = (nal.length >>> 16) & 0xff;\n    out[p + 2] = (nal.length >>> 8) & 0xff; out[p + 3] = nal.length & 0xff;\n    out.set(nal, p + 4); p += 4 + nal.length;\n  }\n  return out;\n}\n\nfunction taRxParseTraf(bytes, traf, tracksById) {\n  const dv = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);\n  const moofStart = traf.moofStart;\n  const tfhd = taRxFindBox(bytes, traf.body, traf.end, \"tfhd\");\n  const tfdt = taRxFindBox(bytes, traf.body, traf.end, \"tfdt\");\n  const truns = taRxBoxes(bytes, traf.body, traf.end).filter((b) => b.type === \"trun\");\n  if (!tfhd || !truns.length) return;\n\n  const tfhdFlags = dv.getUint32(tfhd.body) & 0xffffff;\n  let o = tfhd.body + 4;\n  const trackId = dv.getUint32(o); o += 4;\n  const track = tracksById.get(trackId);\n  if (!track) return;\n\n  let baseDataOffset = moofStart;\n  if (tfhdFlags & 0x01) { baseDataOffset = dv.getUint32(o) * 4294967296 + dv.getUint32(o + 4); o += 8; }\n  if (tfhdFlags & 0x02) o += 4;\n  let defDuration = 0, defSize = 0, defFlags = 0;\n  if (tfhdFlags & 0x08) { defDuration = dv.getUint32(o); o += 4; }\n  if (tfhdFlags & 0x10) { defSize = dv.getUint32(o); o += 4; }\n  if (tfhdFlags & 0x20) { defFlags = dv.getUint32(o); o += 4; }\n\n  let decodeTime = 0;\n  if (tfdt) decodeTime = bytes[tfdt.body] === 1 ? dv.getUint32(tfdt.body + 4) * 4294967296 + dv.getUint32(tfdt.body + 8) : dv.getUint32(tfdt.body + 4);\n\n  for (const trun of truns) {\n    const version = bytes[trun.body];\n    const flags = dv.getUint32(trun.body) & 0xffffff;\n    let t = trun.body + 4;\n    const count = dv.getUint32(t); t += 4;\n    let dataPos = baseDataOffset;\n    if (flags & 0x01) { dataPos = baseDataOffset + (dv.getInt32(t)); t += 4; }\n    let firstFlags = null;\n    if (flags & 0x04) { firstFlags = dv.getUint32(t); t += 4; }\n\n    for (let i = 0; i < count; i++) {\n      let dur = defDuration, size = defSize, sflags = defFlags, cto = 0;\n      if (flags & 0x100) { dur = dv.getUint32(t); t += 4; }\n      if (flags & 0x200) { size = dv.getUint32(t); t += 4; }\n      if (flags & 0x400) { sflags = dv.getUint32(t); t += 4; }\n      if (flags & 0x800) { cto = version === 1 ? dv.getInt32(t) : dv.getUint32(t); t += 4; }\n      if (i === 0 && firstFlags !== null) sflags = firstFlags;\n\n      let data = bytes.subarray(dataPos, dataPos + size);\n      dataPos += size;\n      if (track.kind === \"video\" && track.nalLen !== 4) data = taRxNormalizeNals(data, track.nalLen);\n\n      const sample = { data, dts: decodeTime, duration: dur };\n      if (track.kind === \"video\") { sample.cto = cto; sample.key = !(sflags & 0x00010000); }\n      track.samples.push(sample);\n      decodeTime += dur;\n    }\n  }\n}\n\nfunction taRxIsFragmented(bytes) {\n  for (const b of taRxBoxes(bytes, 0, bytes.length)) if (b.type === \"moof\") return true;\n  return false;\n}\n\nfunction taRxDemuxFmp4(bytes) {\n  const top = taRxBoxes(bytes, 0, bytes.length);\n  const moov = top.find((b) => b.type === \"moov\");\n  if (!moov) throw new Error(\"Fragmented MP4 has no moov (init) segment\");\n\n  const tracksById = new Map();\n  for (const trak of taRxBoxes(bytes, moov.body, moov.end).filter((b) => b.type === \"trak\")) {\n    const t = taRxParseTrak(bytes, trak);\n    if (t) tracksById.set(t.trackId, t);\n  }\n  if (!tracksById.size) throw new Error(\"No decodable tracks in init segment\");\n\n  for (const moof of top.filter((b) => b.type === \"moof\")) {\n    for (const traf of taRxBoxes(bytes, moof.body, moof.end).filter((b) => b.type === \"traf\")) {\n      traf.moofStart = moof.start;\n      taRxParseTraf(bytes, traf, tracksById);\n    }\n  }\n\n  let video = null, audio = null;\n  for (const t of tracksById.values()) {\n    if (t.kind === \"video\" && t.samples.length && !video) video = t;\n    else if (t.kind === \"audio\" && t.samples.length && !audio) audio = t;\n  }\n  return { video, audio };\n}\n\n// ---- MPEG-TS demuxer (secondary path; TorahAnytime uses fMP4) -------\n\nconst taRxPACKET = 188;\nconst taRxSYNC = 0x47;\nconst taRxADTS_SAMPLE_RATES = [\n  96000, 88200, 64000, 48000, 44100, 32000, 24000, 22050,\n  16000, 12000, 11025, 8000, 7350, 0, 0, 0\n];\n\nfunction taRxReadPts(p, off) {\n  const a = (p[off] >> 1) & 0x07;\n  const b = ((p[off + 1] << 8) | p[off + 2]) >>> 1;\n  const c = ((p[off + 3] << 8) | p[off + 4]) >>> 1;\n  return a * 1073741824 + b * 32768 + c;\n}\n\nfunction taRxFindSync(buf) {\n  for (let i = 0; i < buf.length - taRxPACKET; i++) {\n    if (buf[i] === taRxSYNC && buf[i + taRxPACKET] === taRxSYNC) return i;\n  }\n  return buf[0] === taRxSYNC ? 0 : -1;\n}\n\nfunction taRxSplitNals(data) {\n  const nals = [];\n  let i = 0;\n  const n = data.length;\n  let start = -1;\n  while (i < n - 2) {\n    if (data[i] === 0 && data[i + 1] === 0 && data[i + 2] === 1) {\n      if (start >= 0) nals.push(data.subarray(start, data[i - 1] === 0 ? i - 1 : i));\n      start = i + 3;\n      i += 3;\n    } else {\n      i++;\n    }\n  }\n  if (start >= 0 && start < n) nals.push(data.subarray(start, n));\n  return nals;\n}\n\nfunction taRxConcatChunks(chunks, total) {\n  const out = new Uint8Array(total);\n  let o = 0;\n  for (const c of chunks) { out.set(c, o); o += c.length; }\n  return out;\n}\n\nfunction taRxDemuxTs(bytes) {\n  const base = taRxFindSync(bytes);\n  if (base < 0) throw new Error(\"Not an MPEG-TS stream\");\n\n  let pmtPid = -1;\n  let videoPid = -1, audioPid = -1;\n  let videoStreamType = -1, audioStreamType = -1;\n\n  const pes = new Map();\n\n  const video = { type: \"avc\", sps: null, pps: null, width: 0, height: 0, timescale: 90000, samples: [] };\n  const audio = { type: \"aac\", config: null, sampleRate: 0, channels: 0, timescale: 0, samples: [], firstPts: null };\n\n  function startPes(pid) { pes.set(pid, { chunks: [], length: 0 }); }\n  function pushPes(pid, chunk) { const s = pes.get(pid); if (s) { s.chunks.push(chunk); s.length += chunk.length; } }\n\n  function flushVideo(payload) {\n    if (payload.length < 9 || payload[0] !== 0 || payload[1] !== 0 || payload[2] !== 1) return;\n    const ptsDtsFlags = (payload[7] & 0xc0) >> 6;\n    const headerLen = payload[8];\n    if (payload.length < 9 + headerLen) return;\n    let pts = null, dts = null;\n    if ((ptsDtsFlags & 0x2) && payload.length >= 14) {\n      pts = taRxReadPts(payload, 9);\n      dts = (ptsDtsFlags & 0x1) && payload.length >= 19 ? taRxReadPts(payload, 14) : pts;\n    }\n    if (!Number.isFinite(pts)) return;\n    const es = payload.subarray(9 + headerLen);\n\n    const nals = taRxSplitNals(es);\n    const outNals = [];\n    let outBytes = 0;\n    let key = false;\n    for (const nal of nals) {\n      if (nal.length === 0) continue;\n      const t = nal[0] & 0x1f;\n      if (t === 7) { if (!video.sps) { video.sps = nal.slice(); const dim = taRxParseSps(nal); video.width = dim.width; video.height = dim.height; } continue; }\n      if (t === 8) { if (!video.pps) video.pps = nal.slice(); continue; }\n      if (t === 9) continue;\n      if (t === 5) key = true;\n      outNals.push(nal);\n      outBytes += 4 + nal.length;\n    }\n    if (outNals.length === 0) return;\n\n    const sample = new Uint8Array(outBytes);\n    let o = 0;\n    for (const nal of outNals) {\n      sample[o] = (nal.length >>> 24) & 0xff;\n      sample[o + 1] = (nal.length >>> 16) & 0xff;\n      sample[o + 2] = (nal.length >>> 8) & 0xff;\n      sample[o + 3] = nal.length & 0xff;\n      sample.set(nal, o + 4);\n      o += 4 + nal.length;\n    }\n    video.samples.push({ data: sample, pts, dts, key });\n  }\n\n  function flushAudio(payload) {\n    if (payload.length < 9 || payload[0] !== 0 || payload[1] !== 0 || payload[2] !== 1) return;\n    const ptsDtsFlags = (payload[7] & 0xc0) >> 6;\n    const headerLen = payload[8];\n    if (payload.length < 9 + headerLen) return;\n    let pts = null;\n    if ((ptsDtsFlags & 0x2) && payload.length >= 14) pts = taRxReadPts(payload, 9);\n    const es = payload.subarray(9 + headerLen);\n\n    let i = 0;\n    while (i + 7 <= es.length) {\n      if (es[i] !== 0xff || (es[i + 1] & 0xf0) !== 0xf0) { i++; continue; }\n      const protectionAbsent = es[i + 1] & 0x01;\n      const profile = (es[i + 2] >> 6) & 0x03;\n      const srIndex = (es[i + 2] >> 2) & 0x0f;\n      if (srIndex === 15) break;\n      const channels = ((es[i + 2] & 0x01) << 2) | ((es[i + 3] >> 6) & 0x03);\n      const frameLen = ((es[i + 3] & 0x03) << 11) | (es[i + 4] << 3) | (es[i + 5] >> 5);\n      if (frameLen < 7 || i + frameLen > es.length) break;\n      const hdr = protectionAbsent ? 7 : 9;\n\n      if (!audio.config) {\n        const aot = profile + 1;\n        const asc = new Uint8Array(2);\n        asc[0] = ((aot & 0x1f) << 3) | ((srIndex & 0x0f) >> 1);\n        asc[1] = ((srIndex & 0x01) << 7) | ((channels & 0x0f) << 3);\n        audio.config = asc;\n        audio.sampleRate = taRxADTS_SAMPLE_RATES[srIndex] || 44100;\n        audio.channels = channels;\n        audio.timescale = audio.sampleRate;\n      }\n      if (audio.firstPts === null && pts !== null) audio.firstPts = pts;\n\n      audio.samples.push({ data: es.slice(i + hdr, i + frameLen) });\n      i += frameLen;\n    }\n  }\n\n  function flush(pid) {\n    const s = pes.get(pid);\n    if (!s || s.chunks.length === 0) return;\n    const payload = taRxConcatChunks(s.chunks, s.length);\n    if (pid === videoPid) flushVideo(payload);\n    else if (pid === audioPid) flushAudio(payload);\n    s.chunks = []; s.length = 0;\n  }\n\n  for (let p = base; p + taRxPACKET <= bytes.length; p += taRxPACKET) {\n    if (bytes[p] !== taRxSYNC) continue;\n    const b1 = bytes[p + 1], b2 = bytes[p + 2], b3 = bytes[p + 3];\n    const pusi = (b1 & 0x40) !== 0;\n    const pid = ((b1 & 0x1f) << 8) | b2;\n    const afc = (b3 >> 4) & 0x03;\n    if (afc === 0 || afc === 2) continue;\n\n    let off = p + 4;\n    if (afc === 3) off += 1 + bytes[p + 4];\n    if (off >= p + taRxPACKET) continue;\n    const payload = bytes.subarray(off, p + taRxPACKET);\n\n    if (pid === 0) {\n      let idx = pusi ? 1 + payload[0] : 0;\n      idx += 8;\n      if (idx + 4 <= payload.length) {\n        pmtPid = ((payload[idx + 2] & 0x1f) << 8) | payload[idx + 3];\n      }\n      continue;\n    }\n\n    if (pid === pmtPid && videoPid < 0 && audioPid < 0) {\n      let idx = pusi ? 1 + payload[0] : 0;\n      const sectionLen = ((payload[idx + 1] & 0x0f) << 8) | payload[idx + 2];\n      const programInfoLen = ((payload[idx + 10] & 0x0f) << 8) | payload[idx + 11];\n      let es = idx + 12 + programInfoLen;\n      const end = idx + 3 + sectionLen - 4;\n      while (es + 5 <= end && es + 5 <= payload.length) {\n        const streamType = payload[es];\n        const elemPid = ((payload[es + 1] & 0x1f) << 8) | payload[es + 2];\n        const esInfoLen = ((payload[es + 3] & 0x0f) << 8) | payload[es + 4];\n        if ((streamType === 0x1b || streamType === 0x24) && videoPid < 0) { videoPid = elemPid; videoStreamType = streamType; startPes(elemPid); }\n        else if ((streamType === 0x0f || streamType === 0x11) && audioPid < 0) { audioPid = elemPid; audioStreamType = streamType; startPes(elemPid); }\n        es += 5 + esInfoLen;\n      }\n      continue;\n    }\n\n    if (pid === videoPid || pid === audioPid) {\n      if (pusi) flush(pid);\n      pushPes(pid, payload);\n    }\n  }\n\n  flush(videoPid);\n  flush(audioPid);\n\n  if (video.samples.length) {\n    const s = video.samples;\n    for (let i = 0; i < s.length; i++) {\n      let dur;\n      if (i < s.length - 1) {\n        dur = s[i + 1].dts - s[i].dts;\n        if (dur < -4294967296) dur += 8589934592;\n      } else {\n        dur = i > 0 ? s[i - 1].duration : 3000;\n      }\n      s[i].duration = dur > 0 ? dur : 1;\n      s[i].cto = s[i].pts - s[i].dts;\n    }\n  }\n  if (audio.samples.length && audio.config) {\n    const startTicks = Math.round((audio.firstPts || 0) * audio.sampleRate / 90000);\n    for (let i = 0; i < audio.samples.length; i++) {\n      audio.samples[i].dts = startTicks + i * 1024;\n      audio.samples[i].duration = 1024;\n    }\n  }\n\n  const videoOk = video.samples.length && video.sps && video.pps;\n  return {\n    video: videoOk ? video : null,\n    audio: (audio.samples.length && audio.config) ? audio : null\n  };\n}\n\n// ---- progressive MP4 muxer -----------------------------------------\n\nconst taRxMOVIE_TS = 1000;\n\nconst taRxU16 = (n) => Uint8Array.from([(n >> 8) & 255, n & 255]);\nconst taRxU32 = (n) => Uint8Array.from([(n >>> 24) & 255, (n >>> 16) & 255, (n >>> 8) & 255, n & 255]);\nconst taRxS32 = (n) => taRxU32(n < 0 ? n + 0x100000000 : n);\nconst taRxStr = (s) => Uint8Array.from(Array.from(s, (c) => c.charCodeAt(0)));\n\nfunction taRxConcat(arrs) {\n  let size = 0;\n  for (const a of arrs) size += a.length;\n  const out = new Uint8Array(size);\n  let o = 0;\n  for (const a of arrs) { out.set(a, o); o += a.length; }\n  return out;\n}\n\nfunction taRxBox(type, ...children) {\n  const body = taRxConcat(children);\n  return taRxConcat([taRxU32(body.length + 8), taRxStr(type), body]);\n}\n\nfunction taRxFullbox(type, version, flags, ...children) {\n  return taRxBox(type, Uint8Array.from([version, (flags >> 16) & 255, (flags >> 8) & 255, flags & 255]), ...children);\n}\n\nfunction taRxDescriptor(tag, payload) {\n  return taRxConcat([Uint8Array.from([tag, payload.length]), payload]);\n}\n\nfunction taRxEsds(asc) {\n  const decSpecific = taRxDescriptor(0x05, asc);\n  const decConfig = taRxDescriptor(0x04, taRxConcat([\n    Uint8Array.from([0x40, 0x15]),\n    Uint8Array.from([0, 0, 0]),\n    taRxU32(0), taRxU32(0),\n    decSpecific\n  ]));\n  const slConfig = taRxDescriptor(0x06, Uint8Array.from([0x02]));\n  const es = taRxDescriptor(0x03, taRxConcat([taRxU16(0), Uint8Array.from([0]), decConfig, slConfig]));\n  return taRxFullbox(\"esds\", 0, 0, es);\n}\n\nfunction taRxAvc1(track) {\n  const sps = track.sps, pps = track.pps;\n  const avcC = taRxBox(\"avcC\", taRxConcat([\n    Uint8Array.from([1, sps[1], sps[2], sps[3], 0xff, 0xe1]),\n    taRxU16(sps.length), sps,\n    Uint8Array.from([1]),\n    taRxU16(pps.length), pps\n  ]));\n  const entry = taRxConcat([\n    Uint8Array.from([0, 0, 0, 0, 0, 0]), taRxU16(1),\n    taRxU16(0), taRxU16(0), taRxU32(0), taRxU32(0), taRxU32(0),\n    taRxU16(track.width), taRxU16(track.height),\n    taRxU32(0x00480000), taRxU32(0x00480000),\n    taRxU32(0), taRxU16(1),\n    new Uint8Array(32),\n    taRxU16(0x0018), taRxU16(0xffff),\n    avcC\n  ]);\n  return taRxBox(\"avc1\", entry);\n}\n\nfunction taRxMp4a(track) {\n  const entry = taRxConcat([\n    Uint8Array.from([0, 0, 0, 0, 0, 0]), taRxU16(1),\n    taRxU32(0), taRxU32(0),\n    taRxU16(track.channels), taRxU16(16),\n    taRxU16(0), taRxU16(0),\n    taRxU32(Math.min(track.sampleRate, 65535) * 0x10000),\n    taRxEsds(track.config)\n  ]);\n  return taRxBox(\"mp4a\", entry);\n}\n\nfunction taRxStts(durations) {\n  const runs = [];\n  for (const d of durations) {\n    const last = runs[runs.length - 1];\n    if (last && last.delta === d) last.count++;\n    else runs.push({ count: 1, delta: d });\n  }\n  const body = [taRxU32(runs.length)];\n  for (const r of runs) { body.push(taRxU32(r.count), taRxU32(r.delta)); }\n  return taRxFullbox(\"stts\", 0, 0, taRxConcat(body));\n}\n\nfunction taRxCtts(offsets) {\n  const runs = [];\n  for (const o of offsets) {\n    const last = runs[runs.length - 1];\n    if (last && last.offset === o) last.count++;\n    else runs.push({ count: 1, offset: o });\n  }\n  const body = [taRxU32(runs.length)];\n  for (const r of runs) { body.push(taRxU32(r.count), taRxS32(r.offset)); }\n  return taRxFullbox(\"ctts\", 1, 0, taRxConcat(body)); // version 1: signed offsets\n}\n\nfunction taRxStsz(sizes) {\n  const body = [taRxU32(0), taRxU32(sizes.length)];\n  for (const s of sizes) body.push(taRxU32(s));\n  return taRxFullbox(\"stsz\", 0, 0, taRxConcat(body));\n}\n\nfunction taRxStsc(sampleCount) {\n  return taRxFullbox(\"stsc\", 0, 0, taRxConcat([taRxU32(1), taRxU32(1), taRxU32(sampleCount), taRxU32(1)]));\n}\n\nfunction taRxStco(offset) {\n  return taRxFullbox(\"stco\", 0, 0, taRxConcat([taRxU32(1), taRxU32(offset)]));\n}\n\nfunction taRxStss(keyIndices) {\n  const body = [taRxU32(keyIndices.length)];\n  for (const k of keyIndices) body.push(taRxU32(k));\n  return taRxFullbox(\"stss\", 0, 0, taRxConcat(body));\n}\n\nfunction taRxStsd(entry) {\n  return taRxFullbox(\"stsd\", 0, 0, taRxU32(1), entry);\n}\n\nfunction taRxTkhd(id, durationMovie, width, height) {\n  return taRxFullbox(\"tkhd\", 0, 0x000007, taRxConcat([\n    taRxU32(0), taRxU32(0), taRxU32(id), taRxU32(0), taRxU32(durationMovie),\n    taRxU32(0), taRxU32(0), taRxU16(0), taRxU16(0), taRxU16(width ? 0 : 0x0100), taRxU16(0),\n    taRxU32(0x00010000), taRxU32(0), taRxU32(0), taRxU32(0), taRxU32(0x00010000), taRxU32(0), taRxU32(0), taRxU32(0), taRxU32(0x40000000),\n    taRxU32(width << 16), taRxU32(height << 16)\n  ]));\n}\n\nfunction taRxMdhd(timescale, duration) {\n  return taRxFullbox(\"mdhd\", 0, 0, taRxConcat([taRxU32(0), taRxU32(0), taRxU32(timescale), taRxU32(duration), taRxU16(0x55c4), taRxU16(0)]));\n}\n\nfunction taRxHdlr(kind) {\n  const handler = kind === \"video\" ? \"vide\" : \"soun\";\n  const name = kind === \"video\" ? \"VideoHandler\" : \"SoundHandler\";\n  return taRxFullbox(\"hdlr\", 0, 0, taRxConcat([taRxU32(0), taRxStr(handler), taRxU32(0), taRxU32(0), taRxU32(0), taRxStr(name), Uint8Array.from([0])]));\n}\n\nfunction taRxElst(offsetMovie, durationMovie) {\n  const entries = [];\n  if (offsetMovie > 0) entries.push(taRxConcat([taRxU32(offsetMovie), taRxS32(-1), taRxU16(1), taRxU16(0)]));\n  entries.push(taRxConcat([taRxU32(durationMovie), taRxS32(0), taRxU16(1), taRxU16(0)]));\n  return taRxBox(\"edts\", taRxFullbox(\"elst\", 0, 0, taRxConcat([taRxU32(entries.length), ...entries])));\n}\n\nfunction taRxMinf(kind, stbl) {\n  const header = kind === \"video\"\n    ? taRxFullbox(\"vmhd\", 0, 1, taRxConcat([taRxU16(0), taRxU16(0), taRxU16(0), taRxU16(0)]))\n    : taRxFullbox(\"smhd\", 0, 0, taRxConcat([taRxU16(0), taRxU16(0)]));\n  const dinf = taRxBox(\"dinf\", taRxFullbox(\"dref\", 0, 0, taRxU32(1), taRxFullbox(\"url \", 0, 1)));\n  return taRxBox(\"minf\", header, dinf, stbl);\n}\n\nfunction taRxTrak(t) {\n  const videoExtras = [];\n  if (t.kind === \"video\") {\n    videoExtras.push(taRxCtts(t.cttsOffsets));\n    if (t.keyIndices.length) videoExtras.push(taRxStss(t.keyIndices));\n  }\n  const stbl = taRxBox(\"stbl\",\n    taRxStsd(t.kind === \"video\" ? taRxAvc1(t) : taRxMp4a(t)),\n    taRxStts(t.durations),\n    ...videoExtras,\n    taRxStsc(t.sizes.length),\n    taRxStsz(t.sizes),\n    taRxStco(t.chunkOffset)\n  );\n  return taRxBox(\"trak\",\n    taRxTkhd(t.id, t.offsetMovie + t.durationMovie, t.kind === \"video\" ? t.width : 0, t.kind === \"video\" ? t.height : 0),\n    taRxElst(t.offsetMovie, t.durationMovie),\n    taRxBox(\"mdia\", taRxMdhd(t.timescale, t.mediaDuration), taRxHdlr(t.kind), taRxMinf(t.kind, stbl))\n  );\n}\n\nfunction taRxMvhd(durationMovie, nextTrackId) {\n  return taRxFullbox(\"mvhd\", 0, 0, taRxConcat([\n    taRxU32(0), taRxU32(0), taRxU32(taRxMOVIE_TS), taRxU32(durationMovie),\n    taRxU32(0x00010000), taRxU16(0x0100), taRxU16(0), taRxU32(0), taRxU32(0),\n    taRxU32(0x00010000), taRxU32(0), taRxU32(0), taRxU32(0), taRxU32(0x00010000), taRxU32(0), taRxU32(0), taRxU32(0), taRxU32(0x40000000),\n    taRxU32(0), taRxU32(0), taRxU32(0), taRxU32(0), taRxU32(0), taRxU32(0),\n    taRxU32(nextTrackId)\n  ]));\n}\n\n// Unified sample model (works for both MPEG-TS and fragmented-MP4 sources):\n//   video.samples[i] = { data, dts, cto, duration, key }   in video.timescale ticks\n//   audio.samples[i] = { data, dts, duration }              in audio.timescale ticks\n// Cross-track sync is computed in seconds, so each track may use its own timescale.\nfunction taRxMuxMp4(obj) {\n  const video = obj.video, audio = obj.audio;\n  const tracks = [];\n  const starts = [];\n  if (video && video.samples.length) starts.push(video.samples[0].dts / video.timescale);\n  if (audio && audio.samples.length) starts.push(audio.samples[0].dts / audio.timescale);\n  const baseSec = starts.length ? Math.min(...starts) : 0;\n  const toMovie = (sec) => Math.max(0, Math.round(sec * taRxMOVIE_TS));\n\n  if (video && video.samples.length) {\n    const s = video.samples;\n    const durations = s.map((x) => (x.duration > 0 ? x.duration : 1));\n    const cttsOffsets = s.map((x) => x.cto | 0);\n    const sizes = s.map((x) => x.data.length);\n    const keyIndices = [];\n    s.forEach((x, i) => { if (x.key) keyIndices.push(i + 1); });\n    const mediaDuration = durations.reduce((a, b) => a + b, 0);\n    tracks.push({\n      kind: \"video\", id: 1, timescale: video.timescale, width: video.width, height: video.height,\n      sps: video.sps, pps: video.pps, samples: s, durations, cttsOffsets, sizes, keyIndices,\n      mediaDuration,\n      durationMovie: toMovie(mediaDuration / video.timescale),\n      offsetMovie: toMovie(s[0].dts / video.timescale - baseSec)\n    });\n  }\n\n  if (audio && audio.samples.length) {\n    const s = audio.samples;\n    const durations = s.map((x) => (x.duration > 0 ? x.duration : 1024));\n    const sizes = s.map((x) => x.data.length);\n    const mediaDuration = durations.reduce((a, b) => a + b, 0);\n    tracks.push({\n      kind: \"audio\", id: video ? 2 : 1, timescale: audio.timescale,\n      config: audio.config, channels: audio.channels, sampleRate: audio.sampleRate,\n      samples: s, durations, sizes, keyIndices: [],\n      mediaDuration,\n      durationMovie: toMovie(mediaDuration / audio.timescale),\n      offsetMovie: toMovie(s[0].dts / audio.timescale - baseSec)\n    });\n  }\n\n  if (tracks.length === 0) throw new Error(\"No decodable tracks found\");\n\n  let mdatSize = 0;\n  for (const t of tracks) for (const smp of t.samples) mdatSize += smp.data.length;\n\n  const ftyp = taRxBox(\"ftyp\", taRxConcat([taRxStr(\"isom\"), taRxU32(0x200), taRxStr(\"isom\"), taRxStr(\"iso2\"), taRxStr(\"avc1\"), taRxStr(\"mp41\")]));\n\n  const build = () => {\n    const durationMovie = Math.max(...tracks.map((t) => t.offsetMovie + t.durationMovie));\n    return taRxBox(\"moov\", taRxMvhd(durationMovie, tracks.length + 1), ...tracks.map(taRxTrak));\n  };\n\n  // First build to learn moov size, then set real chunk offsets and rebuild.\n  let moov = build();\n  const mdatHeaderStart = ftyp.length + moov.length;\n  let running = mdatHeaderStart + 8;\n  for (const t of tracks) {\n    t.chunkOffset = running;\n    for (const smp of t.samples) running += smp.data.length;\n  }\n  if (running > 0xffffffff) throw new Error(\"File exceeds 4 GB (co64 not supported)\");\n  moov = build();\n\n  const mdatBody = new Uint8Array(mdatSize);\n  let o = 0;\n  for (const t of tracks) for (const smp of t.samples) { mdatBody.set(smp.data, o); o += smp.data.length; }\n  const mdat = taRxConcat([taRxU32(mdatSize + 8), taRxStr(\"mdat\"), mdatBody]);\n\n  return taRxConcat([ftyp, moov, mdat]);\n}\n\n// ---- entry point ---------------------------------------------------\n\nfunction taRxHasBox(bytes, name) {\n  const limit = Math.min(bytes.length - 4, 65536);\n  for (let i = 0; i < limit; i++) {\n    if (bytes[i] === name.charCodeAt(0) && bytes[i + 1] === name.charCodeAt(1) &&\n        bytes[i + 2] === name.charCodeAt(2) && bytes[i + 3] === name.charCodeAt(3)) return true;\n  }\n  return false;\n}\n\nfunction taRxDetectContainer(bytes) {\n  if (bytes.length > 188 && bytes[0] === 0x47 && bytes[188] === 0x47) return \"ts\";\n  if (bytes[0] === 0x47 && bytes.length <= 188) return \"ts\";\n  if (taRxHasBox(bytes, \"ftyp\") || taRxHasBox(bytes, \"styp\") || taRxHasBox(bytes, \"moof\") || taRxHasBox(bytes, \"moov\")) return \"mp4\";\n  return \"ts\";\n}\n\nfunction taRxConcatSegments(segments) {\n  let total = 0;\n  for (const s of segments) total += s.length;\n  const out = new Uint8Array(total);\n  let o = 0;\n  for (const s of segments) { out.set(s, o); o += s.length; }\n  return out;\n}\n\n// initSeg:  Uint8Array | null  — HLS #EXT-X-MAP init (fMP4 ftyp+moov). null for TS.\n// segments: Uint8Array[]       — ordered media segments (.m4s fMP4, or .ts).\n// returns:  Uint8Array         — flat progressive .mp4 (single moov, faststart).\nfunction taRxRemux(initSeg, segments) {\n  const parts = [];\n  if (initSeg && initSeg.length) parts.push(initSeg);\n  if (segments) for (const s of segments) if (s && s.length) parts.push(s);\n  if (!parts.length) throw new Error(\"No segments to remux\");\n\n  const bytes = taRxConcatSegments(parts);\n  const container = taRxDetectContainer(bytes);\n\n  if (container === \"mp4\") {\n    if (taRxIsFragmented(bytes)) {\n      const { video, audio } = taRxDemuxFmp4(bytes);\n      if (!video && !audio) throw new Error(\"No decodable tracks in fragmented MP4\");\n      return taRxMuxMp4({ video, audio });\n    }\n    return bytes; // already a progressive MP4\n  }\n\n  const { video, audio } = taRxDemuxTs(bytes);\n  if (!video && !audio) throw new Error(\"No H.264/AAC tracks found in transport stream\");\n  return taRxMuxMp4({ video, audio });\n}\n";

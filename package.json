{
  "name": "torah-anytime-downloader",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "description": "Desktop app + local proxy for TorahAnytime with a built-in, client-side lecture downloader.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "tauri": "tauri",
    "app": "npm run build:sidecar && tauri build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "PolyForm-Noncommercial-1.0.0",
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.24.0"
  }
}
